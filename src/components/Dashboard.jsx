import React from "react";

export default function Dashboard() {
  return (
    <div>
      <h3>Email Metrics (Coming Soon)</h3>
      <p>Volume, reply time, reply rate % — data will populate here once email tracking is integrated.</p>
    </div>
  );
}
